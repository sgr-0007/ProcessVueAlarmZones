@model ProcessVueAlarmZones.Web.Models.IndexViewModel
@{
    ViewData["Title"] = "EEMUA 191 Zone Classifier";
}

<h1 class="mb-3">@ViewData["Title"]</h1>

<form asp-action="Index" method="post" novalidate>
    @Html.AntiForgeryToken()
    <div class="mb-3">
        <label asp-for="Input.AverageAlarmRate" class="form-label"></label>
        <input asp-for="Input.AverageAlarmRate" class="form-control" type="number" step="any" />
        <span asp-validation-for="Input.AverageAlarmRate" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Input.PercentOutsideTarget" class="form-label"></label>
        <input asp-for="Input.PercentOutsideTarget" class="form-control" type="number" step="any" />
        <span asp-validation-for="Input.PercentOutsideTarget" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Classify</button>
</form>

@if (ViewData.ModelState.IsValid && Model.Result is not null)
{
    var zoneClass = Model.Result.ToString()?.ToLower();
    <div id="zoneBox" class="zone-result @zoneClass mt-4 p-3 rounded">
        <strong>Zone:</strong> @Model.Result
    </div>
}

<script>
    (function () {
        function toggleZone() {
            var box = document.getElementById('zoneBox');
            if (!box) return;
            var hasErrors = document.querySelectorAll('.input-validation-error').length > 0;
            if (hasErrors || box.innerText.trim() === "") {
                box.style.display = 'none';
            } else {
                box.style.display = '';
            }
        }

        document.addEventListener('DOMContentLoaded', toggleZone);
        document.addEventListener('input', toggleZone, true);
        document.addEventListener('change', toggleZone, true);
        document.addEventListener('blur', toggleZone, true);
    })();
</script>